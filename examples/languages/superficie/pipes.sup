def data := [5, 3, 8, 1, 9, 2, 7, 4, 6]

; Thread-last: |>
println(
  data
    |> filter(odd?)
    |> sort()
    |> reverse()
)

; Thread-first: .>
def config := {:host "localhost", :port 3000}
println(
  config
    .> assoc(:port, 8080)
    .> assoc(:debug, true)
)
